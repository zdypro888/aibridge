# 代码知识库(Code Knowledge Base)设计文档

> 状态:概念设计,尚未实现。本文档是多轮讨论的系统性收敛,作为后续推敲与实现的锚点。

## 1. 要解决的问题

AI 在编程中最大的痛点是**上下文丢失(context loss)**:

- **上下文窗口有限且易失**。一万行的项目不可能全部放进上下文;对话进行到第 50 轮时,AI 早已"忘了"第 3 轮读过的 `login.go`。
- **"半忘记"比全忘更危险**。AI 记得有个 `Login` 函数,但忘了签名、参数约定、返回的错误,于是开始**凭印象编造**——猜一个参数顺序、猜一个字段名。写出来的代码看起来合理,实际是错的。
- **理解是"读过即弃"的**。AI 每次花大量 token 读懂一段代码得到的结论,不会沉淀成任何持久的结构化知识。下个会话(甚至同一会话被压缩后)一切从零开始。
- **反复横跳**。AI 把 A 改成 B,过几轮忘了当初为什么改,又把 B 改回 A;多个 AI 协作时甚至互相打架来回改。根源是:**代码只记录"现在是什么",不记录"为什么是这样、曾经否决过什么"**。

把整个代码文件塞进上下文不是解法:一万行读一遍就占掉大半个窗口,且 95% 与当前任务无关。

## 2. 现有方案调研与差距

| 流派 | 代表 | 做法 | 短板 |
|------|------|------|------|
| 按需读取(agentic search) | Claude Code、Codex CLI | 不预索引,grep 定位后只读相关片段,永远以原文为准 | 重复读同样的文件烧 token;上下文压缩(compaction)时理解成果被无差别丢弃 |
| 向量检索(RAG) | Cursor、Windsurf、Copilot | 仓库切块、算 embedding、语义检索相关块 | 按"语义相似"而非"逻辑相关"检索,漏关键调用方;索引滞后于修改 |
| 结构骨架(repo map) | Aider | tree-sitter 提取全项目符号签名,压缩后常驻上下文 | 只有客观结构,没有"怎么用、有什么坑"这类经验知识 |
| 持久笔记 | CLAUDE.md、.cursorrules、memory tool | 跨会话知识文件,启动时注入 | 非结构化、不锚定到具体代码、代码变了笔记不失效 |

**结论:主流最强的 agent 选择"读原文"而不是"总结源码"**,因为总结必然失真和过时。但没有任何一家把"AI 付出理解成本读懂代码后得出的结论"**结构化地沉淀**下来——这是真空地带,也是本工具的定位。

## 3. 核心理念

### 3.1 知识分两层,只有一层值得存

- **客观层**:签名、参数列表、调用关系。可随时从代码推导(AST/LSP 一查就有),存下来反而会过时 → **不存,随时生成**。
- **经验层**:"`password` 传明文,函数内部才做 bcrypt"、"失败 3 次锁账户"、"不要直接调 `Login`,要走 `AuthService.SignIn`"。这是 AI 读了上百行代码**付出理解成本才得到的结论**,代码里没有明写 → **这才是知识库要存的东西**。

### 3.2 读取与总结不是二选一,而是三层配合

1. **骨架层**(自动生成,永不过时):符号地图,解决"忘了什么在哪"。
2. **原文层**(按需读取):精确细节永远以代码原文为准,解决"总结失真"。
3. **结论层**(本知识库):存读代码才能悟出来的东西,锚定到符号、代码一变即标记失效,解决"重复理解"。

### 3.3 最难的问题:AI 不知道自己忘了

遗忘没有自知——AI 凭印象觉得自己"记得",就不会主动去查,然后写错。所以**被动查询不够,必须有主动注入**:AI 要动某个文件/函数时,系统自动把相关知识条目附到它眼前。**查询是补充,注入是保底。**

### 3.4 知识会腐烂,必须可检测

"过时但自信的知识比没有知识更危险"。每条知识必须**锚定**到具体代码并记录写入时该代码的内容哈希;代码一变,哈希对不上,条目自动降级为"可疑,待重新验证",而不是继续当真相提供。

## 4. 空间维度:金字塔结构

知识按代码的自然层级组织成金字塔,**每一层是下一层的摘要**,像书的目录 → 章 → 节 → 段:

```
第1层  项目简介          "本项目是……,核心模块有……"
第2层  文件夹简介        "internal/auth/ 负责登录、会话、权限"
第3层  文件知识点        "login.go:登录入口,含限流和锁定逻辑"
                        (不要太复杂,也不要太简单——一段能建立正确预期的概览)
第4层  函数知识点        "Login(user, pass):怎么调用、参数含义、
                         参数是否会被内部修改、返回什么错误、有什么坑"
第5层  语句/行知识点     "此语句:去掉用户名前后空格"
                        "此语句:对密码做二次加密"
```

### 为什么是金字塔

- **解决"装不下"**:AI 逐层下钻,只展开与当前任务相关的一条路径。项目简介几十 token → 定位文件夹 → 文件 → 函数。其余 9900 行一个 token 都不占。等价于地图的缩放级别:看全国用省级图,找门牌号才放大到街道。
- **失效可局部化**:某行改了,只有该行与所在函数的知识需要重算;文件简介大概率不变,项目简介几乎永远不变。腐烂被限制在局部。

### 关键设计决策

1. **最底层(行/语句级)是稀疏的,不全覆盖。**
   一万行全写注解是维护灾难,且 `i++`、`return err` 这类一眼即懂的行不值得写。只给"惊讶的行"写知识点——**意图从代码本身看不出来、或者改错了会出事的行**(隐藏的业务规则、反直觉的处理、安全相关操作)。函数层往上全覆盖,行层按需点缀。

2. **锚点用内容,不用行号。**
   行号在任何一次插入后全部错位。锚定用语句/函数体的**内容哈希**(或 AST 节点路径),行号仅作辅助显示。代码没变、位置变了 → 知识自动跟着走;代码真变了 → 哈希失配,知识自动标记"待重新验证"。

3. **自底向上生成与更新。**
   函数知识 ← 源码;文件简介 ← 该文件的函数知识汇总;文件夹简介 ← 文件汇总;项目简介 ← 文件夹汇总。每层只看下一层,不直接看源码。改一个函数只需沿"函数 → 文件 → 文件夹 → 项目"一条链向上重算,且传播中经常中途发现"摘要没变"即停。更新成本 **O(深度)** 而非 O(全库)。

4. **函数层知识分两半。**
   - 机器生成(AST 自动算,永不过时):签名、参数类型、调用了谁、被谁调用。
   - AI 生成(读代码悟出来的,宝贵部分):参数会不会被改、隐含约定、坑、正确用法。

## 5. 时间维度:来时路(演化日志)

金字塔的每个节点有**两个面**:

```
Login 函数节点
├── 快照面(现在):怎么调用、参数含义、会不会改参数……
└── 历史面(来时路):
    ├── 2026-05-12  从 session 改成 JWT
    │     原因:多实例部署下 session 不共享
    │     试过被否:Redis 共享 session —— 运维不让加中间件
    ├── 2026-06-03  加了失败 3 次锁定
    │     原因:线上被撞库
    └── 2026-06-20  参数 pass 改为传明文、内部 bcrypt
          原因:之前调用方各自加密,出过两次双重加密 bug
```

### 设计要点

1. **防横跳的核心是"负知识"。**
   最有效的不是记录改了什么,而是记录**否决过什么、为什么否决**。"改回 session"是走回头路、"把 bcrypt 移到调用方"是踩过的坑——这类信息代码里完全不存在,只能靠知识库留下来。

2. **与 git 互补,不重复。**
   git 记录机械事实(哪些行变了),我们记录语义事实(意图、依据、被否决的方案)。粒度也不同:git 的 commit 混多个文件,我们的历史挂在具体节点上——查 `Login` 只看 `Login` 的来时路。git 用来兜底与校验:初建时可从 `git log` 挖历史做初始档案;每条变更记录关联 commit hash,需要机械细节时跳转过去。

3. **写入时机:改代码的动作本身就是写知识的时机。**
   不能指望事后补记。AI 每完成一处修改,工具要求它同时提交一条结构化变更记录:改了什么(一句话)、为什么改(触发原因)、否决了什么替代方案。如同 git commit 必须有 message。**更新日志不是知识库的副产品,它本身是知识库的一等公民**——"知识库更新的过程也要总结成知识库"。

4. **历史也要金字塔化,防止膨胀。**
   活跃函数改 50 次,50 条全量注入又吃满上下文。与空间维度对称:**近期变更保留细节,久远变更压缩成"时代摘要"**("2025 年间此函数经历了 session → JWT 迁移,期间否决过 Redis 方案")。整个系统是两个方向的金字塔:
   - 空间:项目 → 文件夹 → 文件 → 函数 → 行
   - 时间:时代摘要 → 近期变更 → 最新一条的完整细节

## 6. 数据模型(草案)

### 知识节点(空间金字塔的一格)

```yaml
node:
  id:        "internal/auth/login.go#Login"     # 层级路径 + 符号
  level:     function                            # project | dir | file | function | stmt
  anchor:
    file:    "internal/auth/login.go"
    symbol:  "Login"
    hash:    "sha256:ab12…"                      # 写入时该代码单元的内容哈希
    lines:   [40, 140]                           # 仅辅助显示,不作锚定依据
  status:    fresh                               # fresh | suspect(哈希失配,待重验) | stale
  auto:                                          # 机器生成部分(AST,随时可重算)
    signature: "func Login(ctx context.Context, user, pass string, opts *Opts) error"
    calls:     ["bcrypt.GenerateFromPassword", "store.SaveSession"]
    calledBy:  ["AuthService.SignIn"]
  knowledge:                                     # AI 生成部分(经验层,宝贵部分)
    summary:   "登录入口;pass 传明文,内部 bcrypt;失败 3 次锁账户"
    contracts: ["pass 必须是明文", "opts 可为 nil"]
    pitfalls:  ["不要在调用方预先加密——出过双重加密 bug"]
  children:  ["…#Login@stmt:sha256:cd34…"]       # 稀疏的语句级子节点
```

### 变更记录(时间维度的一条)

```yaml
change:
  node:      "internal/auth/login.go#Login"
  at:        2026-06-20T10:32:00Z
  commit:    "9a4851f"                           # 关联的 git commit(可选)
  what:      "参数 pass 改为传明文、内部做 bcrypt"
  why:       "之前调用方各自加密,出过两次双重加密 bug"
  rejected:                                      # 负知识:否决了什么、为什么
    - option: "保持调用方加密,统一封装一个 helper"
      reason: "历史上 helper 被绕过过,治标不治本"
  author:    "claude"                            # 谁改的(哪个 AI / 人)
```

## 7. 与 AI 的交互:三个动作

| 动作 | 触发 | 内容 |
|------|------|------|
| `remember` | AI 读懂一段代码后随手沉淀;修改代码后强制提交变更记录 | 写入/更新节点知识、追加变更记录 |
| `recall` | AI 需要时主动查 | 按符号/文件/关键词查;支持"怎么用"(快照面)与"为什么长这样"(快照+历史)两种模式 |
| **自动注入** | AI 将要读/改某文件、某函数时 | 系统自动附上该节点及祖先链的知识、近期历史。**不依赖 AI 自觉,是防遗忘与防横跳的保底机制** |

查询的两种典型意图:

- **"Login 怎么用?"** → 快照面(签名 + contracts + pitfalls)。
- **"Login 为什么长这样?我想改它。"** → 快照面 + 历史面(含负知识),且在动手前强制注入。

## 8. 生成与维护策略

**初建与生长互相结合:**

1. **打底(全量索引)**:专门跑一次"消化项目"过程,自底向上把塔建起来。机器部分(AST 骨架)成本近乎为零;AI 部分按函数逐个生成(一万行约数百次 LLM 调用,一次性成本)。可从 git 历史挖掘初始的"来时路"。
2. **生长(增量沉淀)**:平时干活时,AI 每读懂一段就 `remember` 一条;每次修改代码即时更新对应节点 + 追加变更记录,并沿金字塔向上传播重算(摘要未变即停)。
3. **腐烂检测**:定期(或每次读取时)校验锚点哈希;失配的节点标记 `suspect`,下次 AI 经过时顺手重验。

**任何变动都必须及时更新知识库**——更新不是定期批处理,而是与编辑动作绑定的同步义务。

## 9. 一句话总结

**一个锚定在代码结构上的双维金字塔知识库**:空间维度逐层摘要,解决"装不下、忘位置";时间维度记录来时路(含负知识),解决"反复横跳";内容哈希锚点,解决"知识腐烂";AI 通过 remember / recall / 自动注入三个动作与它交互;初建全量打底,平时增量生长。

## 10. 待讨论的开放问题

1. **工具形态**:独立 MCP 服务(任何 agent 都能接)、库、还是集成进某个具体系统?
2. **存储介质**:纯文本(JSON/YAML,人可读可改、可进 git)vs SQLite(检索强)?
3. **多语言支持**:锚点哈希与符号提取依赖语言解析,先支持哪些语言(Go 优先?),用 tree-sitter 还是各语言原生 AST?
4. **多 agent 并发**:两个 AI 同时写知识库时的冲突与合并策略。
5. **注入的度**:自动注入多少层祖先、多少条历史,才既够用又不吃爆上下文?
6. **知识质量**:如何防止 AI 存入错误/重复/废话知识(写入校验?同伴复核?)。
